FROM mcr.microsoft.com/playwright:latest

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install dependencies
RUN npm install

# Install Playwright browsers to match installed version
RUN npx playwright install chromium --with-deps

# Copy application files
COPY . .

# Don't start server - tests will connect to web service
CMD ["npx", "playwright", "test"]
